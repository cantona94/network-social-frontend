import { Button } from "@nextui-org/react";

const App = () => {
  return (
    <div className="flex h-screen">
      <Button className="m-auto" color="primary">
        Button
      </Button>
    </div>
  );
};

export default App;
